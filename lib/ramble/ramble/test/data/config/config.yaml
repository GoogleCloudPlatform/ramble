config:
    test_stage: ~/.ramble/test
    overwrite_inventories: false
    spack:
      install:
        flags: --reuse
      concretize:
        flags: --reuse
      env_view:
        link_type: symlink
    include:
      - test_include: include.yaml
    stage_method: 'cp'
