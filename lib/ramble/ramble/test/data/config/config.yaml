config:
    test_stage: ~/.ramble/test
    bootstrap_utilities: false
    overwrite_inventories: false
    spack:
      install:
        flags: --fresh
      concretize:
        flags: --fresh
      env_view:
        link_type: symlink
    include:
      - test_include: include.yaml
    stage_method: 'cp'
